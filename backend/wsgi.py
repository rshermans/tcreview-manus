"""
Ponto de entrada para servidores WSGI (ex.: Gunicorn ou uWSGI).

Ao configurar o Gunicorn, utilize `wsgi:app` para expor a aplicação.
"""
from app import app  # pragma: no cover

# Exporte a aplicação como variável `app` para que o servidor WSGI a descubra.
"""
Ponto de entrada para servidores WSGI (ex.: Gunicorn ou uWSGI).

Ao configurar o Gunicorn, utilize `wsgi:app` para expor a aplicação.
"""
from app import app  # pragma: no cover

# Exporte a aplicação como variável `app` para que o servidor WSGI a descubra.
