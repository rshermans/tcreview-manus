[Unit]
Description=Serviço TrueCheck API (Gunicorn)
After=network.target

[Service]
User=www-data
Group=www-data
WorkingDirectory=/opt/truecheck/backend
EnvironmentFile=/opt/truecheck/backend/.env
ExecStart=/usr/bin/gunicorn -w 4 -b 0.0.0.0:5000 wsgi:app
Restart=always

[Install]
WantedBy=multi-user.target
